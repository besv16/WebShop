@model List<ProjectPreparing.Models.CartViewModel>
@{
    ViewData["Title"] = "Index";
}

<h2>Your Cart</h2>
@foreach (var product in Model)
{
    Html.RenderPartial("Cart", product);
}

@*<button type="submit"><a asp-area="" asp-controller="Checkout" asp-action="Index">Continue to Checkout</a></button>*@
<button type="submit">Continue to Checkout</button>
