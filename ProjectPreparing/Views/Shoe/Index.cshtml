@model List<ProjectPreparing.Project.Core.Models.ShoeViewModel>
@{
    ViewData["Title"] = "Index";
}

<h2>Products</h2>
@foreach (var shoe in Model)
{
    Html.RenderPartial("Shoe", shoe);
}
